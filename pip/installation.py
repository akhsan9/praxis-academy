print "Menginstal dengan get-pip.py"
print "ketik di terminal "python get-pip.py""
print"

2766/5000
Pemakaian:
  pip3 <perintah> [opsi]

Perintah:
  install Instal paket.
  download paket unduhan.
  uninstall Uninstall paket.
  freeze Output paket yang diinstal dalam format persyaratan.
  list Daftar paket yang diinstal.
  show Show informasi tentang paket yang diinstal.
  check Pastikan paket yang diinstal memiliki dependensi yang kompatibel.
  config Mengelola konfigurasi lokal dan global.
  search Cari PyPI untuk paket.
  whheel Bangun roda dari kebutuhan Anda.
  hash Hitung hash arsip paket.
  completion Perintah pembantu yang digunakan untuk menyelesaikan perintah.
  help Tampilkan bantuan untuk perintah.

Pilihan umum:
  -h, --help Tampilkan bantuan.
  --isolated Jalankan pip dalam file
                           mode terisolasi,
                           mengabaikan lingkungan
                           variabel dan pengguna
                           konfigurasi.
  -v, --verbose Memberikan lebih banyak output.
                           Opsi aditif,
                           dan bisa habis
                           sampai 3 kali.
  -V, --version Tampilkan versi dan
                           keluar.
  -q, --quiet Memberi lebih sedikit output.
                           Opsi aditif,
                           dan bisa habis
                           sampai 3 kali
                           (sesuai dengan
                           PERINGATAN, ERROR, dan
                           Pencatatan KRITIS
                           tingkat).
  --log <path> Path ke verbose
                           menambahkan log.
  --proxy <proxy> Tentukan proxy dalam
                           formulir [user: passw
                           d @] proxy.server: port
                           .
  --retries <retries> Jumlah maksimum
                           coba lagi masing-masing
                           koneksi harus
                           upaya (default 5
                           waktu).
  --timeout <sec> Atur soket
                           batas waktu (default 15
                           detik).
  --exists-action <action>
                           Tindakan default ketika
                           jalan sudah
                           ada: penyihir,
                           (i) gnore, (w) ipe,
                           (b) ackup, (a) bort).
  --trusted-host <hostname>
                           Tandai host ini sebagai
                           tepercaya, meskipun
                           tidak ada
"
